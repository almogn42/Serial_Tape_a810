steps to compile

1. in folder enter the command python3 -m venv ./.venv
2. source ./.venv/bin/activate
3.  pip install -r ./requerments.txt
3. python3 -m eel ./eel.py web --icon=./icon.ico --noconsole --windowed --onedir
4. get the .app file from the dist folder 


